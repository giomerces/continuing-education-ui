import { Typography } from "@mui/material";
import styled from "styled-components";

export const StyledTypographyUserName = styled(Typography).attrs({
  variant: "caption",
  component: "div",
})``;

export const StyledTypographyTitle = styled(Typography).attrs({
  variant: "h6",
  component: "div",
})``;
